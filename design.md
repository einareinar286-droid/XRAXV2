# 徐燃安巡 XRAXV2｜Open Design 视觉与交互设计说明

## 0. 一句话设计任务

为“徐燃安巡”设计一套克制、高可信、具有技术感的跨端隐患闭环工作台：使用者必须在任务页面一眼看到自己正处于哪一个环节，并立即知道下一步如何处理。

## 1. 已确定的设计方向

### Signal Path Workstage｜信号路径任务工作台

采用用户已确认偏好的组合：

- **第一版的线条路径图逻辑**：上报、交办、整改、复核、闭环是一条可读、可追踪的任务生命线。
- **第三版的整体工作台布局**：深色矿物质感的中心任务区，左侧路径、右侧证据与审计。
- 质感定位：接近高端工业软件、苹果式信息秩序与精密材质感；不要政府门户蓝、不要廉价渐变、不要赛博朋克网吧特效。

核心隐喻不是“宇宙飞船控制台”，而是“**一项安全任务正在沿着可审计的路径被可靠地推进**”。别把安全隐患系统做成电竞皮肤，事故不会因为霓虹灯多就更容易闭环。

## 2. 设计原则

1. **一项任务，一条路径，一个当前节点。**
2. **先读信息，再感受材质。** 任何背景和特效都不能抢走风险、时限和下一步操作。
3. **高科技感来自秩序、边界、层次与响应，不来自乱发光。**
4. **同一业务，不同端重排。** 小程序与电脑端保留相同的信息优先级和节点含义，不做整体缩放。
5. **重大隐患必须严肃。** 红色只用于风险/阻断，不用于大面积装饰。

## 3. 信息层级

任务详情的阅读顺序固定如下：

```text
1. 任务名称与风险等级
2. 当前节点与下一步行动
3. 责任方、截止时间、地点
4. 整改说明与证据
5. 完整审计记录与辅助信息
```

任意端都应在首屏清楚呈现前 3 项；不能把“当前阶段”藏到时间线底部。

## 4. 布局规范

### 4.1 电脑网页 / 未来电脑客户端（≥1200px）

```text
┌────────────────────────────────────────────────────────────────────────────┐
│ 顶栏：产品名 / 当前位置 / 搜索或筛选 / 用户与角色                              │
├───────────────┬────────────────────────────────────┬───────────────────────┤
│ 左：信号路径    │ 中：任务工作台                       │ 右：证据与审计          │
│               │                                    │                       │
│  ● 上报        │  [重大标记] 任务标题                 │  现场证据缩略图          │
│  │             │  地点 · 责任部门 · 截止时间           │  整改说明                │
│  ● 交办        │                                    │  操作审计时间线           │
│  │             │  当前任务说明                         │                       │
│  ◉ 整改/复核    │  角色对应的主要操作                   │                       │
│  │ 当前步骤     │                                    │                       │
│  ○ 闭环        │                                    │                       │
└───────────────┴────────────────────────────────────┴───────────────────────┘
```

- 左栏宽度稳定，承载纵向路径和当前节点说明。
- 中央区是视觉与操作重心，留出适度呼吸空间。
- 右栏承载可回查信息；在 1280px 以下优先折叠/下移右栏，中央任务区不可被压扁。
- 常规内容最大宽度保持在 1440–1520px 内；超宽屏增加边缘留白。

### 4.2 微信小程序 / 移动端（<768px）

```text
┌──────────────────────┐
│ 顶部导航 / 标题         │
├──────────────────────┤
│ 上报 — 交办 — 整改 — 复核 — 闭环 │
│          ◉ 当前步骤    │
├──────────────────────┤
│ 重大标记 / 任务标题     │
│ 当前状态 + 责任人 + 时限 │
│ 地点                   │
├──────────────────────┤
│ 当前操作 / 整改内容      │
│ 证据                    │
│ 审计记录                │
├──────────────────────┤
│ 安全区上方固定主操作      │
└──────────────────────┘
```

- 节点轨在顶部横向展示，默认保证当前节点可见；必要时允许横向滑动。
- 所有内容单列排列；证据和审计按阅读顺序下放。
- 主操作固定在底部安全区上方，次操作收进更多菜单或内容区。
- 375px 宽下仍应保持节点文字、风险标记和主按钮完整可读。

### 4.3 中间宽度（768–1199px）

- 可使用“两栏：主工作区 + 证据区”或“单列扩展”布局。
- 路径可缩为顶部横向轨/左侧窄轨，但当前节点说明不得消失。
- 不允许把完整三栏简单缩窄到每栏都无法读。

## 5. 生命周期路径组件

### 5.1 节点序列

```text
上报 → 交办 → 整改 → 复核 → 闭环
```

| 节点状态 | 图形与连线 | 文案规则 |
| --- | --- | --- |
| 已完成 | 实心小节点、实线连线 | 显示节点名称和完成时间（有数据时） |
| 当前 | 双层环形节点、细微高亮边缘、实线连接到前一步 | 必有“当前步骤”标签、责任方、下一步提示 |
| 未开始 | 描边空心节点、低对比虚线/弱线 | 保留名称，不伪装成不可见装饰 |
| 退回整改 | 整改节点带退回标识，复核到整改之间呈回流线 | 明示“复核退回”和原因入口 |
| 已闭环 | 全线完成，闭环节点得到稳定的完成标识 | 显示闭环时间与归档状态 |

### 5.2 节点视觉限制

- 当前节点可有一次性 220ms 的轻量强调，不使用永久脉冲圈。
- 节点大小、连线粗细和文字层级必须让色弱用户也能区分状态。
- 重大标记属于任务风险，不替代路径状态；两者必须同时可见。

## 6. 视觉系统

### 6.1 色彩角色

以下是设计 token 的建议起点，Open Design 可微调明度，但须保留语义分工并在实现时验证对比度。

| Token | 建议颜色 | 使用范围 |
| --- | --- | --- |
| `canvas-deep` | `#08110F` | 深色环境底板、背景遮罩基础 |
| `surface-1` | `#111A18` | 主面板 |
| `surface-2` | `#17231F` | 次级面板、悬浮层 |
| `line-soft` | `#2C3C36` | 面板边界、未完成路径 |
| `brand-emerald` | `#006E55` | 主操作、完成状态、品牌锚点 |
| `path-active` | `#65E0D2` | 当前路径的细边、精密高亮 |
| `risk-major` | `#D83A2E` | 重大隐患、阻断风险 |
| `risk-warning` | `#D58B11` | 临期、提醒、次级风险 |
| `text-primary` | `#F3F7F5` | 深色面板主文字 |
| `text-secondary` | `#A8B5AF` | 辅助说明、审计元信息 |

- 整体以深石墨、深矿物绿为底；翡翠绿为行动锚点；青绿色只作精细路径高亮。
- 红色、黄色仅服务于风险与时限，避免把页面做成圣诞树。
- 同时建立浅色维护态 token，以便现有页面或高亮阅读场景平滑过渡；不是强制增加完整暗/亮双主题。

### 6.2 字体与数字

- 中文采用系统无衬线优先：`PingFang SC`、`Microsoft YaHei`、系统默认 sans-serif。
- 避免引入额外在线字体依赖，保证小程序与内网环境可用。
- 任务标题重于视觉标题；数字（时限、数量、日期）使用等宽数字特性或清晰的数字排版。
- 最小正文在移动端不得低于可读级别；辅助信息可以弱化，但不能靠极小字号塞进页面。

### 6.3 材质、圆角与边界

- 面板是“精密、耐用的工作界面”：实体暗面、1px 低对比边界、局部内层次。
- 推荐大面板 20–24px 圆角，小组件 12–16px 圆角；同一层级一致。
- 可以有极浅的层次阴影和细亮边，禁止大面积毛玻璃、厚重投影、彩虹渐变和高频噪点。
- 背景图片只处于最底层，正文一定在稳定阅读面板中。

## 7. 页面设计要求

### 7.1 待办首页

- 首屏给出“今日需处置”、重大任务、临期任务和当前角色。
- 任务卡明确展示：状态、是否重大、责任部门、截止时间、地点、当前步骤。
- 卡片按风险与时限建立阅读优先级，不靠大面积颜色块制造焦虑。
- 空态不是空白：给出当前没有待处理任务的明确说明和下一步入口。

### 7.2 随手拍上报

- 用分组和渐进披露降低填写压力：基本信息、风险判断、现场地点、证据照片。
- 风险等级和重大隐患有清晰说明，不让用户猜缩写。
- 上传中的照片显示进度、失败重试和删除操作；提交按钮反映表单是否可提交。
- M1 Mock 提示与真实生产权限提示应是低干扰但可识别的说明，不可伪装成业务状态。

### 7.3 隐患详情 / 工作台

- 节点路径始终位于首屏高优先级区域。
- 中央任务卡呈现标题、风险、地点、责任部门、责任人、截止时间与当前阶段。
- 角色动作只出现用户有权限的操作：交办、提交整改、复核、闭环、重开等。
- 证据区提供照片、整改说明和时间；审计区按时间记录每一次关键操作。
- 管理层只读时，保留信息完整性，明确“仅查看”而非简单隐藏导致误解。

### 7.4 履职与我的

- 履职页强调“本次记录/历史记录”的工作逻辑，避免堆砌装饰性统计图。
- 我的页清楚展示身份、角色和权限范围；M1 身份切换属于演示信息，生产版本不应继承为真实入口。

## 8. 背景图片替换规范

用户后续会上传背景图片。设计稿需先用标注明确的背景占位，而不是把未授权的现场图混入成品。

| 位置 | 规则 |
| --- | --- |
| 电脑端 | 横向环境图，优先保留左/右侧可裁切区，主任务内容压在独立面板上 |
| 移动端 | 使用竖向裁切或独立移动图；不可直接把横图中间硬裁成手机背景 |
| 文本安全区 | 标题、状态、按钮、风险信息不直接压在图片细节上 |
| 对比度 | 图片之上加入深色/中性色遮罩；复杂图时提高遮罩并保持面板实体化 |
| 隐私 | 现场人员、车牌、设备铭牌、定位信息等先确认脱敏再使用 |

## 9. 交互动效

| 场景 | 动效 | 时长 |
| --- | --- | --- |
| 页面/卡片进入 | 轻微上移 + 渐显 | 180–240ms |
| 当前节点切换 | 环形状态切换、路径线补全 | 220ms 左右 |
| 抽屉/证据展开 | 位移 + 渐显 | 220–280ms |
| 提交成功 | 简短确认反馈 | 180–220ms |

- 仅以 `transform`、`opacity` 为主；不要动画布局尺寸。
- 禁用常驻粒子、扫描线、无限发光、全屏模糊和复杂 3D。
- 应支持“减少动态效果”偏好，降级为静态状态变化。
- 动效是反馈，不是演出；用户正在处理隐患，不是在看片头。

## 10. 需输出的设计稿与组件

### 页面尺寸

- 微信小程序：375×812、430×932。
- 电脑网页：1280×800、1440×900。
- 如有超宽屏展示，补充 1600px 以上的留白规则，而不是另做一套页面。

### 页面与状态

- 待办首页、随手拍上报、任务详情、整改提交、复核/闭环、履职、我的。
- 每个关键页面至少包含：默认、加载、空、异常、无权限；表单另含校验、上传、提交成功与失败。
- 详情页必须覆盖：上报、交办、整改、复核、退回整改、闭环六种可视状态。

### 组件库

- 生命周期路径（桌面纵向 / 移动横向）。
- 风险标签、时限标签、角色标签。
- 任务卡、任务摘要卡、证据卡、审计时间线。
- 主按钮、次按钮、危险操作按钮、只读提示。
- 空态、错误态、无权限态、上传态、骨架屏。
- 色彩、字体、间距、圆角、边界、阴影、动效 token。

## 11. 参考与实施约束

- 产品范围与验收：[`prd.md`](prd.md)。
- 现有工程边界与实现规则：[`tech.md`](tech.md)。
- 既有 UI/交互验收要求：[`docs/spec/03-UI与交互验收-spec.md`](docs/spec/03-UI与交互验收-spec.md)。
- 当前方向预览图：`.impeccable/mocks/07-signal-path-adaptive.png`。

设计交付前需逐项核对：路径是否映射现有状态机、各角色是否只看到有权操作、移动端是否真正重排、背景是否只做环境层。若某个视觉想法需要新状态、新接口、新导航或新依赖，应单独标注为“待产品/技术确认”，不能静悄悄塞进稿子里。

## 12. 潜在风险分析

| 风险等级 | 风险 | 设计应对 |
| --- | --- | --- |
| 高 | 视觉稿新增不存在的流程状态，开发无法对接 | 严格使用上报、交办、整改、复核、退回整改、闭环的既有映射 |
| 高 | 背景图干扰任务阅读或泄露现场信息 | 内容与背景分层；使用遮罩和实体面板；先脱敏再交付 |
| 中 | 深色科技风损害风险可读性 | 红黄仅承担风险语义；所有风险另有文字和图形提示 |
| 中 | 三栏桌面稿缩小后破坏小程序体验 | 单独出移动稿，采用横向路径与单列任务流 |
| 低 | 未来桌面客户端的技术壳变化 | 保持 token、组件与布局规则平台无关 |

本轮第一性原理：已启用。
