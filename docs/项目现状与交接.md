# 徐燃安巡 · 项目现状与交接

更新时间：2026-07-31

## 已完成并已入库

- 私有仓库：`https://github.com/einareinar286-droid/xuzhouanxun`，默认分支 `main`，初始提交 `8ad2ca3`。
- 微信小程序优先的 uni-app Vue 3 工程已建立，`pnpm run build:mp-weixin` 可成功生成 `dist/build/mp-weixin`。
- 演示闭环：隐患随手拍、位置选择、重大隐患高亮、市场部整改提交、安监复核闭环、高管只读查看。
- 履职演示：个人待填/已完成、填报页、管理员总览、状态/部门/分类筛选和分页。
- 履职云端底座：模板、人员动作关系、真实周期实例、审计四张 schema 与 `duty-service` 云对象；设计为本人提交、安监管理员全量查看、无删除接口、按自然日生成实际周期。
- 开发日志、跨端插件核验已放在 `docs`，例行日志下次更新日为 2026-09-29。

## 当前限制（请勿误作已上线能力）

- 小程序构建成功，但微信开发者工具曾出现首屏白屏；需要在其 Console 获取错误并修复后，才能确认界面运行通过。
- 当前身份、任务和履职数据均为演示数据；未接入公司用户名密码、`uni-id`、真实人员、真实台账或生产审计。
- UniCloud 目录只是待部署配置，尚未关联公司服务空间、未上传 schema/云对象、未配置定时任务。
- 图片目前仅在客户端演示选择；未上传云端。现场照片属于敏感业务数据，需公司明确授权并配置私有读写规则后实施。
- 企业微信/钉钉/订阅消息、AI 审核、地图服务、送气工画像 C 类和 Windows 客户端均未接入。

## 接续顺序

1. 修复并验证微信开发者工具中的首屏白屏。
2. 选择并关联公司 UniCloud 服务空间，采用官方 `uni-id-pages`、`uni-id-common` 建立账号与角色。
3. 录入履职模板、人员—动作关系和实际生效日期，部署云对象/schema/定时任务。
4. 经公司授权后配置图片私有存储、微信/钉钉通知和服务端 AI 审核接口。
5. 导入送气工画像的分级字段与规则，单独实现 C 类重点监控页面，不复用或暴露原系统隐私数据。

## 原始业务材料来源

原始参考材料保留在 `C:\Users\Administrator\Desktop\V1`，包括重大隐患判定标准、履职表、隐患整改台账和配送管理办法。工程中只提炼通用字段与规则，不打包原始资料或真实人员数据。
