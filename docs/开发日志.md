# 开发日志

## 2026-07-31｜上游演示工程基线（从 xuzhouanxun/main 导入）

- **目标：** 建立微信小程序优先的 uni-app 演示工程，验证隐患上报、整改、复核闭环与安全履职页面。
- **已有能力：** 隐患随手拍、重大隐患高亮、市场部整改、安全监察部闭环、高管只读；履职页面、个人填报、管理员总览、履职云对象及四张履职 schema。
- **上游验证记录：** 原日志记载微信小程序构建、云对象语法和履职 schema 校验通过；本轮导入不把该历史记录改写为当前环境的重新验证结果。
- **已知边界：** 隐患角色和数据仍由前端 `demoStore` 模拟，尚无生产级登录、授权、审计、私有附件或并发控制。

## 2026-08-02｜模块 02：规格与验收体系初始化

- **目标：** 为徐燃安巡隐患闭环建立独立的规格、里程碑、验收、追溯与发布门禁文档。
- **现状证据：** 当前工作区仅有 `AGENTS.md` 与四份提示词；`README.md`、既有开发日志、`src/`、`uniCloud-aliyun/`、Git 提交及远程仓库均缺失。
- **改动文件：** 新建 `docs/superpowers/plans/2026-08-02-milestone-spec-acceptance.md`、`docs/spec/00-现状证据与边界.md`、`docs/spec/01-业务权限与安全-spec.md`；后续将补齐其余模块 02 文档。
- **关键决策：** 提示词只作为需求依据；没有真实代码、测试、部署或真机记录支撑的条目统一标记为“阻塞/待证”。模块 02 不修改业务实现、UI 或 schema。
- **验证：** 已执行工作区文件清单、Git 状态、分支与预期工程文件存在性检查；结果确认当前目录无目标工程实现与远程仓库。
- **已知风险：** 接入真实仓库前无法核对规格与代码、履职云端兼容、服务端授权和性能；不可宣称任何业务或安全用例已通过。
- **下一步：** 完成研发/性能 Spec、UI/交互验收 Spec、M0–M5 里程碑、用例追溯和变更控制；在提供实际工程后补齐实现与证据链接。

## 2026-08-02｜模块 02：验收链与发布门禁完成

- **目标：** 将共享业务规则拆分为三类 Spec，并建立 M0–M5 里程碑、13 类验收用例、需求追溯矩阵、变更控制和发布门禁。
- **改动文件：** 新建 `docs/spec/02-研发与性能-spec.md`、`docs/spec/03-UI与交互验收-spec.md`、`docs/spec/04-里程碑与发布门禁.md`、`docs/spec/05-验收用例与追溯矩阵.md`、`docs/spec/06-需求变更记录.md`。
- **关键决策：** 以服务端 token、部门/受办范围和状态机作为唯一安全边界；Mock 只用于演示与测试；任何公共契约改动都要有影响、迁移、回滚、验收和审批记录。
- **验证：** 已对 Spec 执行状态、接口、性能、页面与验收用例关键词覆盖检查；检查结果可在当前会话命令输出中定位。当前没有业务工程、测试框架、构建配置、云端部署或真机环境，未执行实现型测试。
- **已知风险：** UC-01 至 UC-13、G1 至 G7 尚无真实证据；目标仓库远程未配置，暂不能推送。
- **下一步：** 执行文档占位/术语/Git 自检，创建本地提交；EE 提供目标工程和远程后回填实现、测试、构建、部署和真机证据并推送。

## 2026-08-02｜模块 02：文档自检完成

- **目标：** 对模块 02 文档执行占位、术语、空白错误和 Git 变更范围检查。
- **验证命令与真实结果：** 占位符扫描无输出；`git diff --check` 无输出。状态术语扫描仅命中“待办”页面名称、“附件上传中”交互反馈、“模块 01 已完成”这一否定性证据列，以及“直接删除隐患”的禁止条款；未发现新增业务状态或允许直接删除的规则。
- **结论：** 文档内部一致性通过；当前没有业务工程、测试框架或真机环境，功能、安全、构建和性能验收仍为阻塞/待证。
- **下一步：** 仅暂存模块 02 新建/维护的文档并创建本地提交；等待 EE 提供远程仓库后推送 `XZAXV2` 分支。

## 2026-08-02｜模块 01：M1 源码基线导入

- **目标：** 在保留模块 02 已有 Spec 的前提下，引入 `einareinar286-droid/xuzhouanxun` 的 `main` 源码，开始 XRAXV2 M1 开发。
- **改动文件：** 导入 `README.md`、包管理文件、`src/`、`uniCloud-aliyun/` 与上游部署说明；新增 M1 设计和实施计划。
- **关键决策：** 上游仓库只读；当前 `XZAXV2` 分支作为隔离开发分支；本地 `AGENTS.md`、三个子项目提示词、共同提示词和下载归档不进入发布集合。
- **验证命令与真实结果：** 公开源码归档下载成功；SHA-256 为 `0AB6CF65EA3AA57B90DF38DCE40AE00A97D5D9DB1B262930014E6C26261FB979`；归档包含 uni-app 页面、履职服务及四张履职 schema。
- **已知风险：** 当前尚未安装依赖或执行本地构建；隐患仍为前端 Mock；Git HTTPS helper 不完整，发布路径需在最终门禁后单独验证。
- **下一步：** 先写隐患领域与 Mock 适配器失败测试，再按 TDD 实现最小闭环。

## 2026-08-02｜XRAXV2 M1 Mock 闭环实现与双端构建

- **目标：** 将隐患前端内存演示升级为具备稳定服务边界、权限校验、状态机、乐观锁、请求幂等和追加式审计的 M1 Mock 闭环；保持原履职模块不变。
- **核心改动：** 新增 `src/domain/issues/` 领域内核、`src/services/issues/` Mock 适配器、`src/stores/issues.js` 异步列表状态；首页、随手拍、详情和“我的”改为调用稳定 Issue 服务；`src/stores/demo.js` 降级为兼容角色入口；版本升级至 `0.2.0`。
- **TDD 记录：** 先建立上报、交办、整改、闭环、退回重提、管理员重开、权限、伪造身份、附件、分页、乐观锁、幂等和审计测试；审计读取越权用例曾按预期失败，补充 `listAuditEvents` 读取策略后转绿。初次门禁为 27 项通过；独立代码审查后增加跨范围安全写入、客户端伪造受办范围、实体通配符和附件元数据用例，均先失败后修复，最终为 30 项通过、0 项失败。
- **依赖安装问题：** 系统 Node v22.11.0 执行多个依赖生命周期脚本时以 Windows `0xC0000005` 异常退出。相同脚本在 Node v22.22.2 下正常；清理仅包含可再生 `node_modules` 的部分安装结构后，使用 Node v22.22.2 与 pnpm 11.9.0 按冻结锁文件安装成功，495 个包和四个生命周期脚本均完成。
- **构建记录：** 微信小程序首次构建通过。H5 首次构建因上游缺少根 `index.html` 失败，补充 uni-app 最小 H5 入口后复验通过。两端均出现 Dart Sass legacy JS API 弃用警告，不影响当前产物，后续依赖升级时处理。
- **安全边界：** 页面持续显示 Mock 标识；身份与部门由当前 Mock 会话解析，忽略写请求中的伪造操作者；审计摘要不包含完整整改正文或附件地址；真实鉴权、私有云附件、AppID、AI 与订阅消息仍不在 M1 范围。
- **代码审查整改：** 安全侧写操作在实体加载后重新校验部门范围；上报的 `departmentScope` 由可信 Mock 规则生成且不接受客户端扩权；实体侧 `*` 不再作为全局通配符；附件仅保存规范字段并拒绝公网预览地址；默认期限按本地自然日计算；首页待办统计排除 `CLOSED`；本地协作文件加入 Git 忽略规则。
- **最终门禁：** 冻结锁文件安装成功；Node 22 测试 30 项通过、0 项失败；微信小程序与 H5 构建成功；发布集合 48 个文件、0 个未暂存修改；5 个本地协作文件均被 Git 忽略；密钥、Token、私钥、本地代理、个人信息模式和 `.env` 扫描均为 0 命中；`git diff --cached --check` 通过。
- **下一步：** 创建公开仓库 `XRAXV2`，保留上游提交历史，将已验证 M1 提交追加至 `main`，再读取远程元数据、提交和代表性文件核验。

## 2026-08-03｜信号路径工作台首批 UI 落地

- **目标：** 在不修改 `IssueService`、状态机、接口和 schema 的前提下，将待办、随手拍和隐患详情统一为“信号路径”跨端体验。
- **核心改动：** 新增 `src/domain/issues/presentation.mjs` 与 `IssueLifecyclePath.vue`；将五个业务状态映射为上报、交办、整改、复核、闭环展示路径。详情页在 1200px 以上使用路径、任务、证据审计三栏，在移动端为单列横向路径；待办卡展示当前步骤与下一责任方；上报页按基本信息、风险判断、地点与证据分组。
- **TDD 记录：** 先写状态路径、退回整改、闭环归档和待整改责任方四项失败测试，再实现展示模型；最终 `pnpm test` 为 34 项通过、0 项失败。
- **验证：** `pnpm run build:mp-weixin`、`pnpm run build:h5` 均成功；H5 本地核验 375×812、430×932、1280×800、1440×900，移动端单列与桌面端三栏均未出现布局溢出；UI 检测器对四个修改目标返回空结果。Dart Sass legacy JS API 弃用警告仍存在，未影响当前产物。
- **安全与边界：** Mock 标识持续显示；页面仅消费既有权限判断和服务接口。真实 token 鉴权、私有附件、服务端审计、弱网、微信开发者工具及真机帧率尚未获得证据，保持阻塞/待证。
- **问题：** 本地 Git 未配置 `user.name` 与 `user.email`，无法创建提交；变更已保留在 `XZAXV2` 工作区，未伪造提交身份。
- **下一步：** 配置或提供 Git 提交身份后创建本地提交；随后进入 M2，接入真实鉴权、后端授权审计与私有附件服务。

## 2026-08-03｜M2 核心 Mock 功能：角色、履职与仪表盘

- **目标：** 完成全员随手拍、市场整改返安监闭环、履职提交与部门审核，以及“履职率必须 100%、未达即考核”的仪表盘主流程。
- **关键决策：** 定位改为可选；员工可上报但不能交办/整改/复核；安监可处理全员隐患，市场仅提交受办整改；履职按时提交且审核通过才计完成，待审核、退回、未提交和逾期提交均进入考核；无任务显示不适用且不进入分母。
- **核心改动：** 增加统一角色定义与本机超级管理员示例配置；隐患 Mock 增加四类角色、可选定位、坐标校验和 Word/Excel/文本/图片/PDF 附件元数据校验；新增履职领域指标、Mock 适配器、待审核项与考核清单；重构履职、履职填报、仪表盘、我的、隐患上报和详情页面。
- **验证：** `pnpm test` 为 45 项通过、0 项失败；`pnpm run build:h5` 与 `pnpm run build:mp-weixin` 均通过；页面静态质量扫描返回空结果。构建中仅有 Dart Sass legacy JS API 弃用提示，不影响产物。
- **边界：** 数据均为匿名 Mock，未导入参考 Excel 或真实人员信息；不含真实手机号登录、记住密码、微信 AppID、短信、真实云端附件或部署。
- **下一步：** 在 HBuilder X/微信开发者工具进行本地交互验收；主功能验收后再单独处理真实账号数据、私有存储和 Cloudflare 测试部署。

## 2026-08-03｜领导工作台与跨端导航

- **目标：** 将首页升级为面向领导快速研判的工作台，并让电脑 H5 使用左侧导航、手机和小程序保留底部导航。
- **关键决策：** 所有首页数字固定标注为“演示数据”，只来源于当前 Mock 会话可见的流程快照；不接入真实公司台账、人员、登录、附件或全球安全资讯。线上默认隐藏 Mock 身份切换入口；此举仅是演示入口控制，不替代真实服务端授权。
- **核心改动：** 新增工作台汇总领域模型和快照服务，展示隐患总数、待交办、待整改、待复核、已闭环、重大隐患，以及履职率、待审核、考核项；重点待办按重大、待复核、期限排序。新增 H5 宽屏左侧文字导航，移动端 Tab 顺序调整为工作台、履职、随手拍、我的。
- **TDD 记录：** 工作台汇总、快照服务、导航契约和页面数据状态均先以失败测试锁定，再实现转绿；新增测试已纳入 `pnpm test`。
- **验证：** `pnpm install --frozen-lockfile` 完成；`pnpm test` 51 项通过、0 项失败；`pnpm run build:h5` 与 `pnpm run build:mp-weixin` 均成功；`git diff --check` 通过；工作台与导航的 Impeccable 静态检测返回空结果。
- **已知边界：** 尚未进行真实账号或真实数据接入，也未重新上传 Cloudflare。Dart Sass legacy JS API 弃用提示仍存在，不影响本次 H5 和小程序产物。
- **下一步：** 由 EE 在 H5 宽屏和微信开发者工具进行一次交互验收；确认后再单独决定是否将本分支构建产物上传到 Cloudflare 测试站。

## 2026-08-04｜真实数据一期范围锁定与 UI 文档草案

- **现状证据：** 当前 `feature/leader-workbench` 分支已完成 Mock 工作台、隐患闭环、履职与考核展示；人员参考表规模约 665 行，履职参考明细约 471 行。参考表仅用于确定后续导入字段，不将其中的滞后业务数据写入 Mock 演示或公开测试站。
- **关键决策：** 当前 Cloudflare 站点继续只作公开 Mock 测试，禁止导入真实姓名、手机号、初始密码、附件和隐患记录；真实数据一期优先完成账号/角色/组织与受办范围、服务端授权、审计和可追溯统计，UI 大改延后至 LIVE 数据链路可用后。
- **新增文档：** `docs/2026-08-04-UI产品需求文档.md` 与 `docs/2026-08-04-UI技术说明.md`，供 EE 先修改和确认；文档未引入数据结构、接口或代码变更。
- **下一步：** EE 评审 UI 草案后，单独评审真实数据一期的人员字段映射、账号初始化、密码散列、私有存储和部署选型；确认后再创建实现计划并开发。

## 2026-08-04｜履职全员仪表盘需求补充

- **新增需求：** 履职模块同时提供个人填报页和全员履职仪表盘。普通员工仅能填写、查看本人任务和历史；安全监察部与 EE 作为超级管理员可按全公司、部门、人员和统计周期查看全部完成情况、待审核、逾期和考核明细；不设置部门管理者。
- **口径：** 全员仪表盘至少展示应履职、按期审核通过、待提交、待审核、退回、逾期和考核人数；履职率固定为“按期审核通过 ÷ 应履职”，目标为 100%。
- **简单操作日志：** 后台增加只读操作日志，记录操作时间、操作人、部门、操作类型、对象、结果和简短备注；安全监察部与超级管理员可查看。日志不保存密码、会话凭据、完整附件地址或敏感正文。
- **边界：** 该需求仅补充产品/技术草案，未导入花名册或履职历史数据，未改变现有 Mock 服务契约、页面或数据库结构。

## 2026-08-04｜真实数据一期方案确认

- **方案：** EE 确认采用“本地预检 + 私有后端预留”方案。Cloudflare 持续只作匿名 Mock 演示；人员源表、真实隐患和附件不上传。
- **导入策略：** 部门分组向下继承；内部 `employeeId` 作为长期身份；手机号仅作规范化匹配与登录入口。重复、异常或无法唯一匹配的手机号进入待核验并禁止登录。
- **功能范围：** 手机号登录与受控会话、人员导入预检、员工本人履职填报、安全监察部/超级管理员全员履职仪表盘、简单操作日志。
- **下一步：** EE 审阅 `2026-08-04-real-data-phase-1-design.md` 后，创建逐项实施计划；在私有部署方案确认前不导入真实数据。

## 2026-08-04｜M3A 本地数据基础能力实施计划获批

- **目标：** 在不接触真实人员、隐患和附件数据的前提下，先完成导入预检、履职全员查看范围、可追溯仪表盘读模型和简易操作日志。
- **关键决策：** 将真实数据一期拆分为 M3A（本地数据基础能力）和 M3B（私有部署、真实认证与本地导入）。Cloudflare 继续只提供匿名 Mock 测试，禁止承载真实业务数据。
- **权限口径：** 普通员工与市场员工仅可填报/查看本人履职；安全监察部与超级管理员可查看全员履职和操作日志；履职率固定为“按期审核通过 ÷ 应履职”，目标 100%，未完成进入考核。
- **实施计划：** `docs/superpowers/plans/2026-08-04-m3-local-data-foundation.md` 已编写，等待按计划进入代码实施；计划未读取、写入或上传花名册及履职源表。

## 2026-08-04｜M3A 本地能力开发

- **完成项：** 新增匿名人员导入预检、履职公司读模型、全员明细、脱敏操作日志和日志页面；管理员履职页展示全员、部门、待审核和考核口径。宽屏左侧导航预留“送气工画像（规划中）”禁用入口，未创建其页面、数据或接口。
- **权限收紧：** 安全监察部与超级管理员可看全公司履职、人员明细和日志；只有超级管理员可审核或改写他人履职，普通/市场员工仅可操作本人履职。
- **数据边界：** 全部数据仍为匿名 Mock；未读取、写入、导入或上传花名册、履职源表、手机号、密码、附件或隐患记录。Cloudflare 未更新。
- **下一步：** 完成完整测试、双端构建、敏感内容检查并推送 `feature/leader-workbench`；M3B 私有部署和真实本地导入保持未执行。

## 2026-08-04｜工作台导航与履职率显示修复

- **修复内容：** 履职指标已是 0—100 的百分比数值，工作台不再二次乘以 100，并强制限制显示范围为 0—100%。
- **导航与权限：** 宽屏安全监察员和超级管理员导航调整为“工作台 → 隐患交办 → 履职仪表盘 → 随手拍”；隐患交办页复用已有隐患详情与交办流程。普通员工及市场人员不显示特权导航。
- **Mock 入口：** 本地开发环境恢复身份切换入口，任意 Mock 身份均可切换回超级管理员；Cloudflare 生产构建默认继续隐藏该入口。
- **验证：** 新增履职率、导航顺序和 Mock 入口回归测试；H5 构建通过。未导入、上传或写入任何真实人员、履职、附件或隐患数据。

## 2026-08-04｜安全履职与仪表盘权限拆分

- **入口：** 全员左侧导航均增加“安全履职”，进入只读取本人任务的个人页；页面标题调整为“我的安全履职”。
- **仪表盘：** “履职仪表盘”仅对超级管理员与安全监察部显示，保留全员查看和既有审核权限；市场营销部与普通员工不显示该入口。
- **顺序：** 特权角色按“工作台、隐患交办、安全履职、履职仪表盘、随手拍”显示；其余角色不显示特权入口。
- **边界：** 未改变个人履职服务的本人范围，也未导入、上传或写入真实业务数据。

## 2026-08-04｜e8ea144 基线全量门禁复核

- **目标：** 验证 feature/leader-workbench 当前提交 e8ea144 的全量门禁，作为后续开发的基线证据。
- **验证命令与真实结果：** pnpm 不在系统 PATH，经 corepack 调用 pnpm 11.20.0；`pnpm test` 70 项通过、0 失败、0 跳过；`pnpm run build:h5` 与 `pnpm run build:mp-weixin` 均输出 DONE Build complete；`git diff --check` 无输出；`git status --short` 为 0 变更，工作区干净。Dart Sass legacy JS API 弃用警告仍存在，不影响当前产物。
- **敏感检查：** 扫描 src/tests/docs 无 .env、密钥、私钥、AppID 或真实手机号命中；唯一命中为操作日志脱敏测试故意构造的 `password=not-kept` 示例，属测试用例而非真实数据。
- **权限影响：** 本轮无代码改动，未触碰权限模型、数据结构、schema、部署方式或任何真实数据。
- **下一步：** EE 指定下一个明确功能后按四步法开发（现状证据 → 失败测试 → 最小实现 → 全量门禁与提交推送）；M3B 私有部署与真实数据导入仍待 EE 确认部署环境与管理边界后单独立项。

## 2026-08-04｜操作日志筛选分页与履职仪表盘人员筛选（G1+G2）

- **目标：** 对齐 2026-08-04 UI 草案差距：操作日志页补齐日期范围、操作人筛选与加载更多分页；履职仪表盘人员明细补齐部门、履职状态、关键词筛选。
- **现状证据：** 服务层已具备全部筛选能力（`listOperationLogs` 支持 `dateRange/actorId/page/hasMore`；`listDutyPeople` 支持 `department/dutyStatus/keyword` 且 `dutyStatus` 值域为 `NOT_APPLICABLE/COMPLETED/ASSESSMENT`），两个页面未暴露控件，属页面层缺口。
- **改动文件：** `src/pages/admin/operation-logs.vue`、`src/pages/admin/duty.vue`、`tests/operation-logs/page-contract.test.mjs`、`tests/duties/admin-page-contract.test.mjs`。
- **G1 实现：** 新增从/到两个 `mode="date"` 日期选择（to 补 `T23:59:59.999Z`）、操作人筛选（选项从已加载日志按 `actorId` 去重）、重置按钮；`hasMore` 时显示"加载更多"按页追加，筛选变更重置回第 1 页。
- **G2 实现：** 全员履职明细区新增部门（选项来自仪表盘部门汇总）、履职状态三档、关键词（姓名/部门/岗位）筛选；人员明细改由 `listDutyPeople` 服务端返回，不做前端裁剪；含筛选加载态、错误态与空态。
- **TDD 记录：** 两个页面契约测试均先确认失败（`mode="date"`、`listDutyPeople` 断言未命中）后实现转绿；同步将过时的 `dashboard.people` 断言更新为 `v-for="person in people"` 新契约。
- **验证：** `pnpm test` 72 项通过、0 失败；`pnpm run build:h5` 与 `pnpm run build:mp-weixin` 均 DONE Build complete；`git diff --check` 无输出。Dart Sass legacy JS API 弃用警告仍存在，不影响产物。
- **权限影响：** 未触碰权限模型与数据结构：日志仍仅安全监察部/超级管理员可读，人员明细仍受 `canViewDutyPeople` 保护；普通员工与市场营销部无入口且服务拒绝。
- **已知边界：** 操作人选项从已加载日志去重（Mock 数据量小足够），真实后端接入后应改为服务端操作人字典；统计周期筛选（G3）仍待 EE 决策周期口径；未导入、上传或写入任何真实业务数据。
- **下一步：** EE 决策统计周期口径（周/月/专项）后补 G3 周期筛选；或评审 UI 草案第 7 节其余待确认项；M3B 私有部署与真实数据导入仍待 EE 确认部署环境后单独立项。

## 2026-08-04｜周期轮换履职任务引擎（Mock）

- **目标：** 履职任务按周期（每日/每周/每两周/每月/每季度/每半年/每年）自动轮换：到期后生成下一周期新任务，旧任务保留为历史并进入考核；填报人=登录账号；任务逐条展示不拼接；节假日前周期暂缓。
- **周期枚举：** `src/domain/duties/periods.mjs` 提供 `PERIOD_TYPES/PERIOD_TYPE_LABELS/nextDueDate/isDueForRollover/nextCycleRange`；按 rolling 语义从当前截止日推进，MONTHLY/QUARTERLY/SEMIANNUAL 做月末安全（2026-01-31 -> 2026-02-28、2024-01-31 -> 2024-02-29、2026-03-31 -> 2026-06-30）；`HOLIDAY_EVE` 预留但调用返回 `NOT_IMPLEMENTED`（需节假日日历依据，暂缓）。
- **轮换语义（EE 确认）：** 到期才轮换（`dueDate < asOf` 触发，当天未过不算）；轮换生成新任务（新 id、PENDING、截止日推进、周期区间连续 `[旧截止日+1, 新截止日]`、cycleKey 更新），旧任务标记 `cycleRolledOver` 保留并继续进入既有考核统计；同一任务不重复轮换；无 `periodType` 的既有任务行为不变。
- **匿名数据边界：** 本机读取 `clipboard-*.xlsx` 两份参考表（665 行花名册、464 行履职明细，含真实姓名/手机号）仅用于提取业务结构（部门 fill-down 后分布、岗位、96 个动作模板、频率分布 每日10/每周9/每两周1/每月301/每季度21/每半年22/每年99/法定假日前1）；提取结果以 14 条匿名周期任务写入 `mock-data.mjs`，覆盖 7 种周期并绑定 4 个 Mock 账号（填报人=登录账号的既有 `ownerUid` 校验不变）；姓名、手机号、Excel 文件一律未进入代码、Git、构建产物。
- **页面：** 个人履职列表任务卡展示周期类型与周期区间（`每两周 · 2026-08-03 至 2026-08-16`），填报页 summary 同口径展示；页面只消费服务返回字段。
- **TDD 记录：** periods 6 项、rollover 6 项、页面契约 3 项均先失败后转绿；旧任务进考核/新任务不计入当前统计、重复读取不重复生成、未到期不轮换、历史提交保留均有测试锁定。
- **验证：** `pnpm test` 86 项通过、0 失败；`pnpm run build:h5` 与 `pnpm run build:mp-weixin` 均 DONE Build complete；`git diff --check` 无输出；敏感扫描对真实姓名、手机号、Excel 文件名为 0 命中。Dart Sass legacy JS API 弃用警告仍存在。
- **权限影响：** 零。未触碰权限模型、schema 与部署方式；`HOLIDAY_EVE` 不启用。
- **下一步：** 节假日前周期需 EE 提供节假日日历依据后再实现；G3 统计周期筛选（周/月/专项）仍待 EE 决策；M3B 真实账号与人员映射、真实导入仍待 EE 确认部署环境后立项。

## 2026-08-04｜移除节假日前周期（HOLIDAY_EVE）

- **决策：** EE 确认节假日前特殊周期先不实现，从代码中彻底移除，不再预留枚举。
- **改动：** `src/domain/duties/periods.mjs` 删除 `PERIOD_TYPES.HOLIDAY_EVE`、对应中文标签与 `NOT_IMPLEMENTED` 分支；`tests/duties/periods.test.mjs` 删除对应用例；实施计划文档同步更新。可用周期固定为每日/每周/每两周/每月/每季度/每半年/每年七种。
- **验证：** `pnpm test` 85 项通过、0 失败；`pnpm run build:h5` 与 `pnpm run build:mp-weixin` 均 DONE Build complete；`git diff --check` 无输出。
- **权限影响：** 零，未触碰权限模型与数据结构。
- **下一步：** G3 统计周期筛选口径待 EE 决策；M3B 真实账号与人员映射、私有部署待 EE 确认环境后立项。

## 2026-08-04｜履职仪表盘周期视图（G3，口径 B）

- **决策：** EE 选定 G3 口径 B——仪表盘按任务自身周期分组统计；顶部周期视图选择器覆盖全部/每日/每周/每两周/每月/每季度/每半年/每年，默认"全部周期"（兼容既有行为）。
- **改动：** `src/domain/duties/dashboard.mjs` 的 `createDutyDashboard` 支持 `periodType` 过滤（任务层过滤后再聚合公司/部门/人员/待审/考核）；`mock-adapter.mjs` 的 `getDutyDashboard({ asOf, periodType })` 与 `listDutyPeople({ department, dutyStatus, keyword, periodType })` 透传；`src/pages/admin/duty.vue` 新增周期视图选择器，切换后总览与人员明细同口径刷新。
- **TDD 记录：** dashboard 过滤（WEEKLY/MONTHLY 混合任务按 MONTHLY 只统计 2 项）、服务层 dashboard/people 过滤、页面契约三处先失败后转绿。
- **验证：** `pnpm test` 88 项通过、0 失败；`pnpm run build:h5` 与 `pnpm run build:mp-weixin` 均 DONE Build complete；`git diff --check` 无输出。Dart Sass legacy JS API 弃用警告仍存在。
- **权限影响：** 零，仍受 `canViewCompanyDutyDashboard/canViewDutyPeople` 保护。

## 2026-08-04｜M3B 真实数据一期决策记录（未实施，仅立项依据）

- **部署环境：** 私有部署选型为火山引擎；Cloudflare 继续仅承载匿名 Mock 测试站，不绑定真实数据。
- **管理员边界：** 超级管理员仅鲁伯贤（EE 本人）一人；安全监察部按既有规则查看全员与审核履职，不授予超级管理员。
- **账号初始化：** 初始密码为手机号，正式版仅保存密码散列；离职人员直接删除账号、禁止登录（真实实现时需确认履职历史记录是否保留供追溯，账号禁用与历史保留可能并存）。
- **异常数据：** 重复/异常手机号进入 `PENDING_VERIFICATION` 名单先行搁置，由 EE 在人员主动联系后逐条核对补入；名单不公开到测试站。
- **数据保留：** 操作日志与审计保留 60 日；附件（后续）存火山引擎私有对象存储，页面仅使用短期访问地址。
- **启动前提：** 需 EE 提供火山引擎访问配置（账号/密钥/桶策略/域名），M3B 才可立项开发；在此之前不导入任何真实人员数据。
