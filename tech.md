# 徐燃安巡 XRAXV2｜技术设计交接说明

| 项目 | 约束 |
| --- | --- |
| 当前技术栈 | uni-app + Vue 3 + Vite + Sass |
| 现有优先端 | 微信小程序（mp-weixin） |
| 同步支持 | H5 电脑网页 |
| 后续方向 | 电脑客户端复用 H5 组件与信息架构 |
| 本轮不做 | iOS 专属端、后端接口重构、状态机变更 |

## 1. 目标

将 Open Design 的视觉稿稳定落到现有 uni-app 项目：同一套任务状态、同一份数据、两种布局。重点是响应式重排和轻量动效，不是将某个平台的画面硬缩到另一个平台。

## 2. 已有工程事实

### 2.1 构建与验证命令

```bash
pnpm test
pnpm run build:mp-weixin
pnpm run build:h5
```

设计落地后，以上命令都应重新执行。真实微信设备、低性能安卓设备和不同桌面浏览器的视觉/交互表现仍需在实现后实测；不能用设计稿或构建成功替代设备验证。

### 2.2 现有页面入口

| 页面 | 现有路径 | 设计对应 |
| --- | --- | --- |
| 待办首页 | `src/pages/index/index.vue` | 总览、待办、重大与临期任务 |
| 隐患上报 | `src/pages/issue/create.vue` | 随手拍上报表单 |
| 隐患详情 | `src/pages/issue/detail.vue` | 信号路径任务工作台 |
| 履职 | `src/pages/duty/index.vue`、`src/pages/duty/record.vue` | 履职概览与记录 |
| 管理履职 | `src/pages/admin/duty/index.vue` | 管理视图 |
| 我的 | `src/pages/profile/index.vue` | 身份、权限、个人入口 |

路由与 tabbar 由 `src/pages.json` 管理；现有 tab 为“待办、随手拍、履职、我的”。设计不应无依据增加一级导航。

### 2.3 既有业务边界

- 服务入口：`src/services/issues/index.mjs`。
- 页面状态：`src/stores/issues.js`。
- 当前为 M1 Mock 适配层，已有稳定的业务接口和版本冲突处理。
- 现有状态机：`REPORTED → ASSIGNED → RECTIFICATION_SUBMITTED → CLOSED`；`RECTIFICATION_SUBMITTED → REJECTED → RECTIFICATION_SUBMITTED`；管理员可由 `CLOSED → REPORTED` 重开。
- 角色：`SAFETY_INSPECTOR`、`SAFETY_ADMIN`、`MARKETING_RECTIFIER`、`EXECUTIVE_READONLY`。

视觉改造只能消费这些状态和权限，不得通过前端文案或假按钮制造未实现的业务能力。

## 3. 响应式架构

### 3.1 断点与布局策略

| 视口 | 目标端 | 页面布局 |
| --- | --- | --- |
| `< 360px` | 紧凑型手机 | 单列；压缩次要说明；主操作保持可点击 |
| `360–767px` | 微信小程序/手机网页 | 单列；顶部横向节点路径；底部固定主操作 |
| `768–1199px` | 平板、小尺寸电脑窗口 | 两栏或单列扩展；证据/审计收为折叠区或次级区 |
| `≥ 1200px` | 电脑网页、未来桌面客户端 | 左路径 + 中央任务 + 右证据审计三栏 |
| `≥ 1600px` | 大屏办公显示器 | 内容最大宽度受控，增加留白，不无限拉长阅读行 |

推荐电脑端工作区最大宽度为 1440–1520px。具体值可随设计稿确定，但页面不可在超宽屏上被拉成难以阅读的三条长河。

### 3.2 电脑端栅格

```text
容器（max-width: 1440–1520px）
├─ 路径栏：240–280px，保持可见
├─ 主工作区：minmax(0, 1fr)，优先分配宽度
└─ 证据审计栏：300–360px，可在中等宽度收拢
```

- 三栏使用 CSS Grid/Flex 的可重排能力，禁止靠绝对定位固定整体版式。
- 中等宽度下先将右侧栏下移/折叠，再考虑缩减路径栏；不能同时把三栏压窄。
- 未来桌面客户端继续使用该布局，无需预先选择 Electron、Tauri 或其他桌面壳。

### 3.3 移动端规则

- 优先使用 uni-app 的弹性布局、`rpx` 与安全区域变量；H5 补充媒体查询。
- 可点击区域最小高度建议 44px（小程序约 88rpx），相邻高风险操作留足间距。
- 主操作位于底部安全区上方；附件、审计记录按阅读顺序下排。
- 不依赖鼠标悬停展示关键状态。移动端不做桌面侧栏的缩略版。

## 4. 任务路径组件

建议抽取为可复用的“生命周期路径”组件，输入为既有业务状态而不是页面临时文案。

```text
输入：status、role、deadline、isMajor、reviewReason
输出：节点状态、当前节点说明、下一步操作提示、退回整改提示
```

### 4.1 节点映射

| 业务状态 | 路径当前节点 | 必要补充 |
| --- | --- | --- |
| `REPORTED` | 上报 | 待管理员交办 |
| `ASSIGNED` | 交办 | 责任部门、责任人、整改时限 |
| `RECTIFICATION_SUBMITTED` | 整改 | 整改说明、证据、待复核 |
| `REJECTED` | 整改（带退回标识） | 退回原因、再次整改提示 |
| `CLOSED` | 闭环 | 完成时间、归档状态 |

- 完成节点、当前节点、未来节点、退回状态应有结构化样式名，不能只在模板中写散落的颜色判断。
- 电脑端纵向、移动端横向可以是同一数据模型的不同布局。
- 节点顺序和状态含义必须与服务层一致；视觉层不得新增“处理中”“验收中”等无对应枚举的永久状态。

## 5. 样式与组件策略

### 5.1 Token 优先

现有 `src/uni.scss` 已有如下基础 token：

```scss
$xr-green: #006e55;
$xr-red: #d83a2e;
$xr-amber: #d58b11;
$xr-ink: #17201d;
$xr-muted: #69756f;
$xr-line: #dce4e0;
```

设计落地时应扩展为语义 token（画布、面板、主文字、次文字、边界、当前路径、风险、警告、成功），集中管理。不得在各页面散落复制色值、阴影和圆角。

### 5.2 技术选择

- 继续使用原生 uni-app/Vue 能力与现有 `uni-ui`，本轮不为了视觉效果新增重型动画、图表或 3D 依赖。
- 优先 CSS Grid/Flex、伪元素、SVG/CSS 线条完成路径与材质效果。
- 避免依赖在微信小程序中支持不一致的复杂滤镜、全局 `backdrop-filter`、Canvas/WebGL 粒子和混合模式。
- 图标使用现有可用图标体系或统一 SVG 资源，避免不同页面混用线性、面性和 emoji 图标。

## 6. 背景图片接入规则

背景图由用户后续提供。接入时按“环境层”实现：

```text
背景图片 → 深色/中性色遮罩 → 内容容器 → 文字与操作
```

- 背景应使用可替换的本地/受控资源引用，不把图片内容写死在组件逻辑中。
- 为电脑横版、移动竖版分别定义 `object-position` 或裁切位；同一张图不可盲目居中裁切。
- 任务文字、风险标记和操作按钮不能直接压在复杂照片上。
- 页面首屏优先加载文字和操作，背景采用优化尺寸、按需加载；附件图片采用缩略图/预览分层。
- 生产接入前需确认图片版权、现场敏感信息与隐私脱敏。

## 7. 动效与性能预算

- 普通状态切换时长：180–300ms，默认 220ms。
- 仅使用 `transform`、`opacity` 为主的过渡；避免动画 `width`、`height`、`top`、`left` 等布局属性。
- 当前节点可做一次性轻量入场与状态切换强调，不做持续脉冲、粒子、扫描光或全屏发光。
- 加载中使用简洁骨架屏或局部指示器；不遮蔽已可用内容。
- 为系统“减少动态效果”偏好提供静态/短时长降级方案。
- 性能目标需在真机和目标浏览器上实测后记录；本设计说明不预设未经测量的帧率结论。

## 8. 可访问性与交互

- 重大、临期、退回、完成等状态同时使用文字、图标/形状与颜色。
- 电脑端可键盘聚焦主操作、表单、附件和折叠区；焦点样式清晰。
- 文本与背景、按钮与面板应在实现时进行对比度检查。
- 表单错误直接关联到字段，提交结果给出明确可读反馈。
- 删除、重开、闭环等影响状态的操作保持二次确认和操作结果提示。

## 9. 设计落地后的验证清单

- [ ] `pnpm test` 通过。
- [ ] `pnpm run build:mp-weixin` 通过。
- [ ] `pnpm run build:h5` 通过。
- [ ] 检查 375px、430px、1280px、1440px 视口的溢出、遮挡与主操作可达性。
- [ ] 检查五个业务节点和退回整改在各端显示一致。
- [ ] 检查四类角色在各状态下只看到其有权操作。
- [ ] 检查加载、空、错误、无权限、上传、校验、冲突等状态。
- [ ] 使用真实背景图后检查对比度、裁切、隐私和加载体验。

## 10. 实施边界

Open Design 的交付应提供可映射到本说明的组件、token 与状态标注。前端实施前，如设计需要新增状态、接口字段、路由、依赖或桌面壳技术选择，须单独评审，不能在视觉开发中顺手改掉——这种“顺手”最容易把项目顺手送进沟里。

本轮第一性原理：已启用。
