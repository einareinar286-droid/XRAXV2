# 徐燃安巡

## 跨平台安全隐患闭环与安全履职产品需求文档

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 徐燃安巡 |
| 公司 | 徐州中燃能源有限公司 |
| 文档版本 | V1.0 |
| 文档用途 | 功能开发、接口设计、测试验收、Stitch 原型设计输入 |
| 首期平台 | PC 网页端、微信小程序 |
| 后续平台 | Android 客户端、Windows 桌面客户端；必要时补充 iOS/分享页面 |
| 前端技术约束 | uni-app Vue 3，优先使用官方跨端 API 与 uni-ui |
| 首期业务重点 | 安全监察部与市场营销部之间的隐患交办、整改、复核和闭环 |
| 数据规模 | 全公司约 600 人，日常高频使用预计少于 100 人 |

> 本版本先定义功能和业务规则，视觉风格、颜色、动效和最终组件由后续 Stitch 原型确定。Stitch 原型不得改变本文档规定的权限、状态和数据留痕规则。

---

## 1. 产品目标

### 1.1 业务目标

徐燃安巡用于把燃气安全管理中的现场发现、交办、整改、复核、抽查和履职记录统一到一个可追溯系统中。

核心结果：

1. 每一项隐患都有唯一编号、责任部门、责任人、整改期限和处理状态。
2. 重大隐患在所有相关列表和统计中优先显示，并明确提示风险。
3. 市场营销部提交整改证据后，自动进入安监待复核事项的前端。
4. 安监部能看到逾期、临期、待复核和未交办事项，并在系统内提醒相关人员。
5. 所有提交、退回、复核和闭环动作保留审计记录，不提供删除或覆盖历史记录的业务入口。
6. 每名员工可以填写自己的安全履职周期，不能查看或修改其他员工的个人填报内容。
7. 安监管理员可以按人员、部门、类别、周期和状态查看全员履职情况。

### 1.2 首期成功标准

- 安监管理员能登录并创建一条隐患交办单。
- 市场整改人员只看到分配给自己或市场营销部的交办事项。
- 市场整改人员能上传文字说明和现场佐证后提交复核。
- 安监管理员能退回整改或确认闭环。
- 普通员工不能进入整改单处理页面，不能创建交办、提交整改或复核闭环，但仍可按权限使用随手拍和本人履职。
- 高管可以只读查看重点隐患和进度，不显示任何编辑、提交、退回或闭环按钮。
- PC 网页与微信小程序使用相同的状态、权限和接口规则。
- 构建失败、网络失败、无数据、无权限和服务端异常都有明确反馈，不出现空白页面。

### 1.3 非目标

首期不实现以下生产能力，但为后续保留接口或页面入口：

- 真实用户名密码服务、统一身份认证和企业组织通讯录同步。
- 现场照片正式云存储和面向员工的公开分享链接。
- AI 自动审核的实际模型调用。
- 微信订阅消息、短信、钉钉工作通知或企业微信通知。
- 送气工画像 C 类重点监控页面。
- 关码数据的自动导入与自动降级任务。
- Windows 原生安装包和 Android 应用商店发布包。
- 复杂财务、考勤、薪资和客户经营管理功能。

---

## 2. 用户、组织和权限

### 2.1 组织范围

系统支持以下部门：

- 安全监察部
- 市场营销部
- 财务管理部
- 综合管理部
- 生产运营部

部门用于账号归属、任务分配、数据筛选和统计维度。部门本身不是权限的唯一依据，后端必须结合角色、任务归属和数据范围进行校验。

### 2.2 角色定义

| 角色标识 | 中文名称 | 默认部门 | 说明 |
| --- | --- | --- | --- |
| `SYSTEM_ADMIN` | 管理员 | 安全监察部 | 用户本人；拥有系统、权限、模板和全局安全数据管理权限 |
| `SAFETY_ADMIN` | 安监管理员 | 安全监察部 | 负责隐患交办、复核、抽查和履职总览 |
| `MARKETING_OPERATOR` | 市场整改人员 | 市场营销部 | 处理分配给市场营销部的整改任务 |
| `EMPLOYEE` | 普通员工 | 任意部门 | 可随手拍、查看授权内容、填写本人履职 |
| `EXECUTIVE_VIEWER` | 高管只读 | 任意部门 | 查看重点隐患、进度和汇总，不改变数据 |

### 2.3 整改单权限矩阵

| 能力 | 管理员 | 安监管理员 | 市场整改人员 | 普通员工 | 高管只读 |
| --- | --- | --- | --- | --- | --- |
| 进入整改单工作区 | 是 | 是 | 是 | 否，默认只读摘要 | 只读重点范围 |
| 新建隐患交办单 | 是 | 是 | 否 | 否 | 否 |
| 随手拍上报 | 是 | 是 | 可上报 | 是 | 按配置决定，默认只读 |
| 指定责任部门/责任人 | 是 | 是 | 否 | 否 | 否 |
| 修改整改期限 | 是 | 是 | 否 | 否 | 否 |
| 查看市场部收到的任务 | 全量 | 全量 | 本部门/本人 | 不可进入处理工作区 | 授权的重点摘要 |
| 提交整改说明 | 是，可代办 | 是，可代办 | 仅本人/本部门任务 | 否 | 否 |
| 上传整改佐证 | 是，可代办 | 是，可代办 | 仅本人/本部门任务 | 否 | 否 |
| 退回整改 | 是 | 是 | 否 | 否 | 否 |
| 确认复核闭环 | 是 | 是 | 否 | 否 | 否 |
| 查看审计时间线 | 全量 | 全量 | 本任务处理记录 | 授权摘要 | 授权摘要 |
| 删除或覆盖历史记录 | 否 | 否 | 否 | 否 | 否 |

> “其他人员只能查看、不能修改、不能使用整改单处理功能”只限制整改单模块。普通员工仍需要使用随手拍和填写自己的安全履职，这是两个不同的权限范围。

### 2.4 权限校验要求

1. 前端根据角色隐藏不允许的导航、按钮和表单。
2. 后端云对象或服务端必须再次校验角色、部门和任务归属。
3. 用户通过深链接打开无权限页面时，显示无权限页，不返回空白页。
4. 角色切换只允许在演示环境使用，生产环境禁止前端自行切换角色。
5. 账号停用后，已登录会话应在下一次请求时失效。

---

## 3. 平台和产品形态

### 3.1 PC 网页端

- 用于安监管理员、管理员和市场整改人员的高频工作。
- 左侧固定主导航在切换页面后保持存在。
- 主内容区支持宽屏列表、筛选、详情和双栏处理面板。
- 支持键盘 Tab 导航、焦点状态、批量查看和分页。
- 推荐支持 1280px 以上完整布局，1024px 以上可用布局。
- 不依赖 Windows 原生 SDK；后续可使用 Electron 或 WebView 封装，但业务页面不改写。

### 3.2 微信小程序

- 用于现场随手拍、查看待办、提交整改证据、填写履职和接收页面内提醒。
- 使用底部 Tab 导航、单列卡片、吸顶筛选和安全区适配。
- 所有定位、相册、相机能力必须通过 uni-app 跨端 API 调用。
- 需要用户授权时显示原因、授权失败后的替代路径和重试入口。

### 3.3 Android 与 Windows 后续适配

- 页面路由、权限、接口、状态枚举和表单字段与 PC/小程序保持一致。
- 原生能力单独封装在平台适配层，不把 `wx.*` 或 Android SDK 直接写入业务组件。
- 文件、定位、通知、分享能力定义统一接口，按平台实现适配器。
- 客户端离线时只允许保存未提交草稿，不能伪造已提交或已闭环状态。

---

## 4. 功能模块总览

### P0 首期必须实现

1. 登录与会话
2. 角色权限和部门数据范围
3. 待办首页
4. 隐患随手拍
5. 整改单列表
6. 隐患详情与时间线
7. 安监交办
8. 市场整改提交
9. 安监退回与复核闭环
10. 系统内消息中心
11. 我的安全履职
12. 履职填报
13. 安全履职总览
14. PC 固定左侧导航
15. 加载、空状态、错误、无权限和提交中状态

### P1 接口和页面骨架

1. 安监真实情况抽查专区
2. 检查表模板中心
3. 用户、部门、角色和账号状态管理
4. 线下检查表记录导入
5. 关码数据导入接口
6. 消息规则配置

### P2 预留能力

1. 送气工画像 A/B/C 页面
2. AI 图片与文字审核
3. 微信订阅消息或其他通知渠道
4. Windows 和 Android 客户端封装
5. 经营安全趋势报表和导出

---

## 5. 页面需求

### 5.1 登录页

**访问角色：** 全员。

**模块：**

- 公司名称和产品名称。
- 用户名输入框。
- 密码输入框。
- 登录按钮。
- 密码规则提示。
- 登录失败提示。
- 会话失效提示。
- 演示环境身份选择，仅开发环境启用。

**规则：**

- 生产环境不能显示角色切换器。
- 密码不写入本地明文存储。
- 登录成功后服务端返回用户身份、部门、角色和权限版本。
- 登录失败应避免暴露“用户名不存在”等敏感信息。
- 连续失败达到服务端阈值后需要冷却或二次验证。

### 5.2 待办首页

**访问角色：** 全员，内容按角色过滤。

**模块：**

- 今日待处理数量。
- 重大隐患数量。
- 已逾期数量。
- 待安监复核数量。
- 本期已闭环数量。
- 角色标签和当前部门。
- 闭环待办列表。
- 重大隐患置顶。
- 临期和逾期标签。
- 快捷入口：随手拍、整改单、消息中心、我的履职。

**排序：** 重大隐患优先，其次逾期，再按整改期限升序，最后按创建时间倒序。

**角色差异：**

- 安监/管理员：查看全量未闭环事项。
- 市场整改人员：查看本部门被交办事项。
- 普通员工：查看授权的公开摘要，不显示处理操作。
- 高管：查看重大隐患和关键进度摘要。

### 5.3 隐患随手拍页

**访问角色：** 管理员、安监管理员、市场整改人员、普通员工；高管默认只读。

**字段：**

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 隐患标题 | 是 | 最大 60 个字符 |
| 隐患详细描述 | 是 | 现场情况、风险和建议 |
| 隐患类别 | 是 | 用户用气安全、配送作业、厂站设备、管网设施、安全管理等 |
| 是否重大隐患 | 是 | 默认否；需用户确认判定依据 |
| 现场图片 | 否，生产建议至少 1 张 | 当前仅演示本地选择；正式上线需私有云存储 |
| 位置来源 | 是 | 当前位置或地图自选位置 |
| 地址文本 | 条件必填 | 选择位置成功后显示 |
| 纬度/经度 | 条件必填 | 仅授权后保存，不对无权限人员公开 |
| 责任部门 | 安监创建时可直接填 | 普通员工上报后由安监补充 |
| 责任人 | 安监创建时可直接填 | 默认市场营销部负责人候选 |
| 整改期限 | 安监创建时可填 | 普通员工上报后由安监设置 |

**提交规则：**

- 普通员工提交后状态为“待交办”。
- 安监或管理员提交后可以直接进入“待整改”。
- 隐患编号由服务端生成，前端不得自行决定正式编号。
- 提交成功后跳转详情页，并显示后续处理路径。
- 提交失败保留表单内容和本地草稿。

### 5.4 整改单列表页

**访问角色：** 安监管理员、管理员、市场整改人员；普通员工和无授权人员不进入处理工作区。

**筛选：**

- 状态：待交办、待整改、待复核、已逾期、已闭环。
- 风险：全部、重大隐患、一般隐患。
- 责任部门。
- 责任人。
- 区域。
- 创建日期和整改期限。
- 关键字：编号、标题、用户、用户代码、地址。

**列表卡片字段：** 编号、重大隐患标签、状态、标题、责任部门、责任人、整改期限、位置、最后操作人、最后更新时间。

**规则：**

- 市场整改人员只能看到本部门或本人负责的任务。
- 安监和管理员可以查看全量。
- 列表按风险和期限排序。
- PC 支持分页；小程序支持触底加载。
- 不提供删除按钮。

### 5.5 隐患详情页

**模块：**

1. 隐患摘要：编号、标题、类别、重大标记、状态。
2. 责任信息：责任部门、责任人、检查人、上报人。
3. 期限信息：创建时间、整改期限、剩余天数、逾期天数。
4. 现场信息：地址、位置来源、图片、隐患描述。
5. 交办要求：整改建议、通知单状态、安监备注。
6. 整改材料：文字说明、整改结果、佐证图片、提交时间。
7. 闭环时间线：每次动作、人员、时间和备注。
8. 操作区：根据权限显示交办、提交整改、退回、复核闭环。

**不可变规则：**

- 原始上报内容不可被整改人员修改。
- 整改提交后，市场人员不能覆盖历史提交；退回后生成新的处理动作。
- 闭环后不允许再次编辑；如发现问题，管理员新建后续隐患或复查任务。

### 5.6 安监交办

**访问角色：** 管理员、安监管理员。

**字段：** 责任部门、责任人、整改期限、整改建议、是否发送系统提醒、是否公开只读摘要。

**规则：**

- 首期责任部门重点为市场营销部。
- 交办成功后状态改为“待整改”。
- 交办动作写入审计时间线。
- 市场营销部收到消息中心提醒。
- 交办单不能删除。

### 5.7 市场整改提交

**访问角色：** 市场整改人员、管理员、安监管理员代办。

**字段：** 整改措施、实际完成时间、整改结果、现场说明、现场图片、补充备注。

**规则：**

- 只允许处理本人或市场营销部被分配的任务。
- 文字说明为必填。
- 生产环境可要求至少一张整改图片，具体由模板配置决定。
- 提交后状态为“待复核”。
- 提交后市场人员只能查看，不能覆盖本次提交。

### 5.8 安监退回与复核闭环

**访问角色：** 管理员、安监管理员。

**退回：**

- 退回原因必填。
- 状态回到“待整改”或“已逾期”。
- 市场整改人员收到系统内提醒。
- 退回原因写入时间线。

**闭环：**

- 安监核对整改文字、图片、实际风险和期限。
- 复核意见可选，重大隐患建议必填。
- 闭环确认后二次确认，防止误操作。
- 状态改为“已闭环”，记录闭环人和闭环时间。
- 高管和统计数据实时更新。

### 5.9 消息中心

**首期渠道：** 仅系统内消息和待办提醒，不接钉钉。

**消息类型：** 被交办、即将到期、已逾期、整改退回、待复核、已闭环、权限变更、系统通知。

**字段：** 消息编号、消息类型、标题、正文、关联业务编号、接收人、创建时间、已读时间、跳转路径。

**规则：**

- 消息按接收人的权限生成。
- 点击消息后标记已读并跳转到关联详情。
- 逾期消息不自动删除。
- 已读和未读状态不影响业务状态。
- 后续通知渠道由服务端消息适配器扩展。

### 5.10 我的履职

**访问角色：** 全员。

**模块：** 本期完成率、任务总数、待填写、已完成、已逾期、真实周期、任务列表、周期截止日。

**个人数据边界：**

- 用户只能查询自己的履职实例。
- 用户只能提交自己的未完成实例。
- 用户不能修改人员、部门、频率、周期开始日和截止日。
- 用户不能删除或覆盖历史记录。

### 5.11 履职填报

**字段：** 履职说明、检查范围、发现问题、问题数量、现场图片、附件说明、提交时间。

**状态：** 待填写、已逾期、已提交/已完成、审核中（后续可选）。

**规则：**

- 截止日由真实自然日期计算，不使用演示轮替日期。
- 周期结束后自动创建下一周期实例。
- 旧周期不覆盖、不删除、不重新计算成新周期。
- 任务模板变更只影响后续周期。
- 提交失败可保存草稿，但只有服务端确认后才变为已完成。

### 5.12 安全履职总览

**访问角色：** 管理员、安监管理员；高管只读查看汇总。

**筛选：** 人员、部门、岗位、岗位类型、履职类别、频率、周期、状态、截止日期范围。

**统计：** 总任务数、已完成数、完成率、逾期数、本期完成率、部门排名、人员状态灯。

**列表字段：** 姓名、部门/站点、岗位、动作名称、类别、频率、周期、截止日、状态、最后填报时间、逾期天数。

### 5.13 安监真实情况抽查专区

**首期页面骨架，P1 完整上线。**

**模块：** 抽查计划、抽查对象、检查日期、检查人、现场情况、发现问题、图片、关联隐患、抽查结论、复查日期。

**规则：**

- 只有安监管理员和管理员可创建或修改抽查记录。
- 发现问题时可一键创建隐患草稿。
- 抽查记录不可删除，只能作废并填写原因。

### 5.14 检查表模板中心

**访问角色：** 管理员、安监管理员。

**模块：** 模板分类、模板名称、版本号、字段配置、必填配置、照片要求、适用部门/岗位、频率、启用/停用、历史版本。

**规则：**

- 模板内容修改不改变已生成任务。
- 发布新版本后只应用于新周期。
- 停用模板不删除历史任务。
- 模板版本必须可追溯。

### 5.15 个人与管理设置

**员工可见：** 姓名、部门、岗位、当前角色、消息偏好、退出登录、隐私说明。

**管理员可见：** 账号、部门、角色、状态、数据范围、最后登录时间、密码重置、停用/启用。

---

## 6. 隐患状态机和流程

### 6.1 状态枚举

| 状态 | 含义 | 可进入状态 |
| --- | --- | --- |
| `PENDING_ASSIGNMENT` | 已上报，等待安监交办 | `PENDING_RECTIFICATION` |
| `PENDING_RECTIFICATION` | 已交办，等待责任人整改 | `PENDING_REVIEW`、`OVERDUE` |
| `OVERDUE` | 已超过期限仍未闭环 | `PENDING_RECTIFICATION`、`PENDING_REVIEW` |
| `PENDING_REVIEW` | 整改材料已提交，等待安监复核 | `PENDING_RECTIFICATION`、`CLOSED` |
| `CLOSED` | 已复核闭环 | 不允许修改；发现新问题时新建记录 |
| `VOIDED` | 管理员作废的错误记录 | 仅保留审计，不进入正常待办 |

### 6.2 标准流程

```text
随手拍/安监录入
      ↓
待交办
      ↓ 安监指定市场营销部、责任人、期限
待整改 ──超过期限──> 已逾期
      ↓ 市场提交整改材料
待复核
  ├─退回并填写原因──> 待整改/已逾期
  └─确认复核──────> 已闭环
```

### 6.3 重大隐患规则

重大隐患判定以公司提供的《城镇燃气经营安全重大隐患判定标准》为业务参考。前端只负责展示和采集确认，不替代专业人员最终判断。

重大隐患包括但不限于：安全管理制度缺失、厂站监测报警或切断设施失效、管线占压、不合规气瓶、违规燃具、地下密闭空间用气、书面告知整改措施缺失等。后续应将判定条款配置为可维护的检查项。

重大隐患的系统行为：

- 列表置顶。
- 使用独立风险标签。
- 首页单独统计。
- 详情页显示判定依据和复核提醒。
- 临期和逾期消息提升优先级。
- 高管只读看板默认展示。

---

## 7. 安全履职周期规则

### 7.1 业务字段来源

参考《徐州中燃安全职责履职表》：姓名、部门/站点、岗位、岗位类型、手机号、履职动作、总动作数、已完成数、完成率、状态灯、最后填报时间、最近截止日、逾期任务数、本期完成数、本期完成率。

### 7.2 周期规则

- 周期频率支持每日、每周、每两周、每月、每季度、每半年和每年。
- 周期按中国自然日和真实日期计算。
- 任务到期后由服务端定时任务生成下一周期。
- 生成新周期时不修改旧周期的开始日、截止日、说明或提交内容。
- 服务端定时任务需要幂等，重复执行不能产生重复实例。
- 离职或停用人员不再生成新的周期，但历史任务继续保留。
- 任务截止日由模板频率和生效日期计算，不允许前端伪造周期。

### 7.3 履职数据权限

- `myInstances`：只按当前 token 的用户 ID 返回本人实例。
- `submitInstance`：服务端再次校验实例 ownerUid 与当前用户一致。
- `adminInstances`：仅管理员和安监管理员可调用。
- 高管只读接口只返回汇总和授权范围，不返回员工私密填报原文和原始图片。
- schema 禁止客户端删除。

---

## 8. 数据模型

### 8.1 用户与组织

```text
User
- id
- username
- displayName
- departmentId
- departmentName
- stationName
- jobTitle
- jobType
- roleIds[]
- mobileMasked
- status: ACTIVE | INACTIVE | LOCKED
- lastLoginAt
- passwordChangedAt
- createdAt
- updatedAt
```

### 8.2 隐患记录

```text
Issue
- id
- issueNo
- title
- description
- category
- severity: NORMAL | MAJOR
- status
- reporterUid
- reporterName
- reporterDepartment
- locationText
- latitude
- longitude
- locationSource: CURRENT | MAP_PICKER | MANUAL
- source: QUICK_REPORT | INSPECTION | DUTY | IMPORT
- responsibleDepartmentId
- responsibleUserUid
- deadline
- rectificationSuggestion
- notificationIssued
- publicSummary
- createdAt
- updatedAt
- closedAt
- closedBy
```

### 8.3 整改记录

```text
Rectification
- id
- issueId
- submitterUid
- submitterName
- note
- result
- completedAt
- attachments[]
- version
- createdAt
```

### 8.4 审计记录

```text
IssueAudit
- id
- issueId
- action
- fromStatus
- toStatus
- actorUid
- actorName
- actorRole
- note
- metadata
- createdAt
```

### 8.5 附件

```text
Attachment
- id
- ownerType: ISSUE | RECTIFICATION | DUTY | INSPECTION
- ownerId
- storageKey
- fileName
- mimeType
- size
- width
- height
- sha256
- visibility: PRIVATE | AUTHORIZED
- createdBy
- createdAt
```

附件默认私有。前端只接收短期签名 URL 或服务端代理 URL，不保存永久公开链接。

### 8.6 履职模板、分配和周期实例

现有 UniCloud schema 已覆盖以下四类数据：

- `xr-duty-templates`：动作名称、类别、频率、字段、照片要求、版本和启用状态。
- `xr-duty-assignments`：人员、部门、岗位动作的关系、生效日期、ownerUid。
- `xr-duty-instances`：真实周期、截止日、状态、提交内容和附件。
- `xr-duty-audit`：查看、提交、退回、周期生成等操作记录。

---

## 9. 前端接口约定

接口名称仅作为统一服务层契约，具体使用 uniCloud 云对象或公司后端实现。

### 9.1 会话接口

```text
POST /auth/login
输入：username, password
输出：accessToken, refreshToken, user, roles, permissions, expiresAt

POST /auth/logout
GET  /auth/me
POST /auth/refresh
```

### 9.2 隐患与整改单接口

```text
POST /issues
GET  /issues
GET  /issues/:id
POST /issues/:id/assign
POST /issues/:id/rectification
POST /issues/:id/review/return
POST /issues/:id/review/close
GET  /issues/:id/audits
```

列表查询参数：status、severity、departmentId、assigneeUid、category、keyword、deadlineFrom、deadlineTo、page、pageSize。

服务端返回统一结构：

```json
{
  "data": [],
  "page": 1,
  "pageSize": 20,
  "total": 0,
  "requestId": "..."
}
```

错误结构：

```json
{
  "code": "FORBIDDEN",
  "message": "当前账号无权处理此整改单",
  "requestId": "..."
}
```

### 9.3 消息接口

```text
GET  /messages
POST /messages/:id/read
POST /messages/read-all
```

消息服务按业务权限生成，不允许前端自行给其他用户发送系统消息。

### 9.4 履职接口

```text
GET  /duty/my-instances
GET  /duty/my-instances/:id
POST /duty/instances/:id/submit
GET  /duty/admin-instances
GET  /duty/statistics
```

### 9.5 模板和抽查接口

```text
GET  /duty/templates
POST /duty/templates
POST /duty/templates/:id/publish
POST /duty/templates/:id/disable
GET  /inspections
POST /inspections
POST /inspections/:id/close
```

### 9.6 送气工画像预留接口

首期不显示主导航入口，但服务层保留：

```text
GET  /drivers
GET  /drivers/:id
POST /drivers/import-lock-events
POST /drivers/monthly-downgrade/preview
POST /drivers/monthly-downgrade/confirm
```

字段需兼容公司现有资料：关码人员、关码日期、关码原因、整改完成日期、开码日期、网格长、累计关码次数、严重度、最后关码日期、离职状态、等级和冷却期。

分级规则预留为：关码 0 次为 A，1 次为 B，2 次及以上为 C；严重度达到重大阈值可直接升为 C；最后关码距今超过 60 天可在管理员确认后降一级；降级后进入 30 天冷却期。规则最终以公司确认版本为准。

### 9.7 AI 审核预留接口

```text
POST /ai/issue-review
输入：issueId, text, attachmentIds[]
输出：status, riskItems[], suggestions[], confidence, modelVersion
```

AI Key、模型地址和隐私数据处理均在服务端完成，禁止写入小程序包、PC 前端代码或提交到 GitHub。

---

## 10. 状态、空状态和异常处理

### 10.1 加载状态

- 首屏显示页面骨架，不显示空白背景。
- 列表使用卡片骨架和加载更多状态。
- 统计卡片可独立加载，不能阻塞普通任务列表。
- 提交按钮进入提交中状态，防止重复提交。
- 上传文件显示单文件进度、失败原因和重新上传入口。

### 10.2 空状态

| 场景 | 文案方向 | 操作 |
| --- | --- | --- |
| 没有待办 | 当前没有待处理事项 | 返回首页或随手拍 |
| 没有整改单 | 当前筛选没有整改单 | 清除筛选 |
| 没有消息 | 当前没有需要处理的消息 | 返回待办 |
| 没有履职任务 | 当前周期没有任务 | 联系安监管理员 |
| 无筛选结果 | 没有匹配记录 | 清除筛选 |
| 无权限 | 当前账号无权访问此功能 | 返回可访问页面 |
| 未登录 | 登录后查看工作台 | 去登录 |

### 10.3 异常

- 网络错误：显示重试按钮和 requestId。
- 会话过期：保留当前页面上下文，跳转登录后返回原页面。
- 服务端拒绝：显示权限说明，不使用通用“加载失败”。
- 表单校验错误：字段附近显示具体原因，不只使用 Toast。
- 图片定位权限拒绝：解释用途，提供手动地址或暂存草稿路径。
- 重复提交：服务端使用幂等键返回原结果。
- 服务端定时任务失败：管理员消息中心显示运维告警，不修改前端状态。

---

## 11. PC 和微信小程序适配要求

### 11.1 共享部分

- 共享 Vue 页面、服务层、数据模型、权限判断和状态枚举。
- 共享业务校验规则和错误码。
- 共享消息中心和任务时间线。
- 共享附件元数据，不共享平台临时文件路径。

### 11.2 PC 专属表现

- 左侧导航在页面切换后保持。
- 列表与详情支持双栏或侧滑面板。
- 支持鼠标悬停、键盘焦点和较大的筛选区域。
- 支持导出按钮的权限占位，但首期可暂不接文件导出。

### 11.3 小程序专属表现

- 底部 Tab：待办、随手拍、履职、我的。
- 操作按钮放在拇指可达区域。
- 适配 375px、390px、430px 宽度。
- 使用 `uni.chooseImage`、`uni.getLocation`、`uni.chooseLocation` 等跨端 API。
- 图片、定位和地图授权都要有用户说明。

### 11.4 Android/Windows 兼容约束

- 业务页面不能直接调用微信 API。
- 文件、定位、通知和分享必须经过 `platform` 服务层。
- 不在页面中读取平台特有的永久路径。
- 平台能力不可用时返回 `supported: false` 和可读提示。
- Windows 先支持响应式网页，再评估 Electron 封装。

---

## 12. 安全、隐私和审计

### 12.1 账号安全

- 密码服务端存储，使用成熟密码哈希算法。
- 密码至少 10 位，包含字母、数字和特殊字符；最终规则由公司安全策略确定。
- 登录失败限速、IP/账号风险控制和会话过期由服务端实现。
- accessToken 不写入 URL，不写入日志，不提交到分析服务。
- 管理员操作需要更短会话周期和可选二次验证。

### 12.2 数据安全

- 真实人员、手机号、现场照片和地址不得提交到演示仓库。
- 现场图片采用私有存储和授权读取。
- 经纬度不对无关人员公开；高管只读视图默认显示区域级地址。
- AI 调用只在服务端进行，API Key 不进入前端。
- 所有敏感字段按最小权限返回。

### 12.3 审计

必须记录：登录、退出、失败登录、查看敏感详情、创建隐患、交办、整改提交、退回、复核、闭环、模板发布、角色变更和账号停用。

审计记录至少包括操作者、角色、时间、业务编号、原状态、新状态、IP/设备摘要、操作备注和 requestId。审计记录不可由普通前端删除。

---

## 13. 验收测试

### 13.1 权限测试

- 以管理员登录，可创建交办、退回、闭环、查看所有履职和管理设置。
- 以安监管理员登录，可完成完整隐患流程，但不能管理系统管理员账号。
- 以市场整改人员登录，只能看到本部门任务；打开其他任务 API 返回 FORBIDDEN。
- 以普通员工登录，看不到整改单处理入口；深链访问显示无权限；仍可随手拍和填本人履职。
- 以高管登录，只读查看授权汇总；所有编辑按钮和提交接口均不可用。
- 前端隐藏按钮后，使用接口或深链仍不能绕过后端权限。

### 13.2 闭环测试

- 新建隐患后生成唯一编号。
- 普通员工上报进入待交办。
- 安监交办后市场收到系统消息，状态变为待整改。
- 市场提交说明后状态变为待复核。
- 安监退回时必须填写原因，状态回到待整改。
- 安监确认后状态变为已闭环，列表和统计同步更新。
- 已闭环记录不能编辑、删除或覆盖。
- 重大隐患在首页、列表和高管汇总中置顶或高亮。

### 13.3 履职测试

- 每人只能查到自己的周期实例。
- 周期按真实日期生成。
- 提交失败不显示已完成。
- 提交成功后历史实例只读。
- 定时任务重复执行不生成重复周期。
- 管理员能按部门、类别、状态、周期筛选全部人员。
- 模板新版本不改变历史记录。

### 13.4 平台测试

- PC 1280px、1024px 和窄窗口下布局可用。
- 微信小程序 375px、390px 和 430px 下无横向溢出。
- 页面切换后 PC 左侧导航仍存在，当前菜单状态正确。
- 小程序底部 Tab 正常切换。
- 定位、相册和地图拒绝授权后仍可完成可允许的后续操作。
- H5 构建和微信小程序构建均成功。

---

## 14. 实施顺序

### 阶段一：可验收演示

1. 完成登录演示、五类角色切换和前端权限表现。
2. 完成隐患随手拍、交办、整改、复核、闭环和时间线。
3. 完成首页、整改单列表、详情、消息中心和 PC 固定导航。
4. 完成个人履职、履职填报和管理员总览演示。
5. 完成空状态、加载、错误和无权限状态。
6. 生成 PC H5 和微信小程序构建产物。

### 阶段二：云端生产基础

1. 关联公司 UniCloud 服务空间。
2. 配置官方 `uni-id-pages` 和 `uni-id-common`。
3. 上传履职 schema 和 `duty-service` 云对象。
4. 导入人员、岗位动作、模板和真实生效日期。
5. 配置每日周期生成、逾期计算和审计。
6. 配置账号安全和角色数据范围。

### 阶段三：生产附件和管理能力

1. 经公司授权后启用私有图片存储。
2. 启用真实隐患台账、抽查和模板管理。
3. 增加消息规则和可选微信订阅消息。
4. 接入关码导入和送气工画像接口。
5. 执行安全、隐私、压力和跨端测试。

### 阶段四：客户端扩展

1. 复用 uni-app 页面适配 Android。
2. 复用 PC H5 页面封装 Windows 桌面端。
3. 逐项验证定位、图片、通知、文件和分享能力。
4. 根据平台审核要求补充隐私声明和权限说明。

---

## 15. 当前实现与本文档的差异

当前代码已经具备可运行的演示页面和主要流程，但以下内容仍不可视为生产上线：

- 演示身份切换不等于真实账号登录。
- 演示数据不等于公司真实隐患和人员台账。
- 图片目前仅在客户端选择，尚未正式上传云端。
- AI、订阅消息、真实抽查和送气工画像尚未接入。
- UniCloud schema 和云对象需要关联公司服务空间后部署。
- 当前 PC/微信小程序用于功能验证，视觉设计由后续 Stitch 原型重新确定。

任何开发任务完成后，应同步更新 `docs/开发日志.md`；项目正式使用后每 60 天更新一次简短版本日志，内容包含版本、关键变更、已知风险和下一步。

---

## 16. 参考材料

原始材料位于：`C:\Users\Administrator\Desktop\V1`

- 《徐州中燃安全职责履职表.xlsx》
- 《隐患整改台账线上线下 .xlsx》
- 《开关码明细.xlsx》
- 《送气工名单.xls》
- 《送气工画像_分级规则.docx》
- 《城镇燃气重大隐患判定标准.docx》
- 《瓶装液化石油气配送服务管理办法6.2(1).docx》

原始文件不直接打包进小程序，不在前端暴露真实人员隐私。字段、规则和接口采用最小必要原则提炼。
