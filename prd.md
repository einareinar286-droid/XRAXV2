# 徐燃安巡 XRAXV2｜产品需求文档（PRD）

| 项目 | 内容 |
| --- | --- |
| 版本 | M1 设计交接版 |
| 面向对象 | Open Design、产品、前端开发 |
| 设计方向 | Signal Path Workstage（信号路径任务工作台） |
| 优先平台 | 微信小程序、电脑网页；未来复用至电脑客户端 |
| 暂不纳入 | iOS 手机端、真实生产身份认证、真实云端部署 |

## 1. 产品目标

徐燃安巡用于燃气安全隐患的上报、交办、整改、复核与闭环。界面的首要职责不是“看起来像个大屏”，而是让使用者在 3 秒内确认：这是什么任务、是否重大、当前卡在哪一步、谁要处理、下一步做什么。

本轮设计采用“**信号路径 + 中央任务工作台 + 证据审计侧栏**”的结构：任务全生命周期以明确节点呈现，当前节点必须一眼可辨；电脑端承载更高的信息密度，微信小程序以单列、触控优先的同一信息架构呈现。

## 2. 范围与目标用户

### 2.1 本轮范围

- 隐患列表与待办总览。
- 随手拍上报隐患。
- 隐患详情、流转、整改、复核、闭环。
- 证据、处理记录与审计轨迹查看。
- 履职记录与个人中心。
- 微信小程序、电脑网页、未来电脑客户端的响应式设计规则。
- 用户后续提供背景图片后的替换规范。

### 2.2 用户与权限

| 角色 | 主要工作 | 关键界面能力 |
| --- | --- | --- |
| 安全检查员 | 发现、上报、跟进隐患 | 查看待办、随手拍、补充信息 |
| 安全管理员 | 分派、复核、闭环、重开 | 交办、复核、闭环、重开、查看审计 |
| 营销整改人 | 接收任务、整改并提交证据 | 接收交办、提交整改说明与照片 |
| 管理层只读 | 查看进度和风险 | 查看任务、证据、审计；无写操作 |

权限必须清晰影响可操作项：没有权限时展示原因或只读状态，不把无权限按钮伪装成可点击装饰。

## 3. 任务生命周期

系统已有的业务状态机是设计的唯一事实来源，设计不得另造一套“好看但无法落地”的状态。

| 状态 | 中文节点 | 当前处理方 | 关键动作 | 视觉要求 |
| --- | --- | --- | --- | --- |
| `REPORTED` | 上报 | 安全管理员 | 交办 | 起始节点已点亮，提示待交办 |
| `ASSIGNED` | 交办 | 营销整改人 | 提交整改 | 交办节点为当前节点，显示责任部门与时限 |
| `RECTIFICATION_SUBMITTED` | 整改 | 安全管理员 | 复核 | 整改节点为当前节点，突出整改证据 |
| `REJECTED` | 退回整改 | 营销整改人 | 再次提交整改 | 在路径上明确显示“复核退回”的回流，不可伪装为新任务 |
| `CLOSED` | 闭环 | 无 | 查看归档 | 全路径完成，闭环节点有明确完成感 |

状态流转：

```text
上报 → 交办 → 整改 → 复核 → 闭环
                  ↑       │
                  └─ 退回整改
```

## 4. 核心体验要求

### 4.1 明确的节点路径

- 每一个任务详情页均展示完整的 5 节点路径：上报、交办、整改、复核、闭环。
- 已完成、当前、未开始、退回整改必须使用不同的节点样式和连线状态，不能只靠颜色区分。
- 当前节点需有环形高亮和“当前步骤”文字标签；同时给出当前责任方、截止时间和下一步操作。
- 退回整改必须显示复核结果与退回原因，并将路径明确回指到整改节点。
- 移动端使用顶部横向节点路径；电脑端使用左侧纵向节点路径。两端状态名称、顺序、含义一致。

### 4.2 任务工作台

任务详情是核心页面。电脑端以三栏工作台呈现：

```text
┌────────────┬─────────────────────────────┬──────────────────┐
│ 信号路径     │ 任务主工作区                  │ 证据与审计         │
│ 当前节点     │ 标题、风险、地点、时限、下一步  │ 照片、处理记录、时间线 │
└────────────┴─────────────────────────────┴──────────────────┘
```

- 中央区域始终优先呈现任务摘要和本角色下一步动作。
- 右侧区域展示照片、整改说明、操作记录和审计时间线。
- 任务为重大隐患时，风险级别、截止时间和升级处理提示必须高优先级出现。
- 页面不可用纯装饰效果遮挡任务信息；任务处理效率高于视觉炫技。

### 4.3 主要页面

| 页面 | 用户目标 | 必须信息/动作 |
| --- | --- | --- |
| 待办首页 | 找到下一件要处理的任务 | 待处理数量、重大任务、临期任务、任务列表、筛选与刷新 |
| 随手拍上报 | 快速记录隐患 | 标题、描述、分类、风险等级、地点、照片、提交状态 |
| 隐患详情 | 判断任务状态并完成下一步 | 节点路径、任务摘要、责任人、时限、证据、审计、角色对应操作 |
| 履职 | 记录或查看履职事项 | 履职状态、记录入口、历史记录 |
| 我的 | 查看个人身份与设置 | 角色、权限说明、个人信息、入口集合 |

### 4.4 必须覆盖的状态

每个关键页面均需给出以下状态稿或组件规则：

- 加载中、空列表、网络/服务异常、无权限。
- 表单校验、上传中、上传失败、提交成功。
- 版本冲突或数据已更新后的刷新提示。
- 重大隐患、临期、逾期、已闭环、复核退回。
- 管理层只读状态。

## 5. 跨端产品规则

### 微信小程序

- 单列阅读和单手触控优先，主操作固定在安全区域上方。
- 节点路径横向排列，可横向滑动但默认应看到当前节点。
- 不依赖 hover、右键、快捷键或超小文字传达关键信息。
- 不通过整体等比缩小电脑端页面来适配；信息要重新编排。

### 电脑网页

- 面向 1280px 以上工作屏，优先支持 1440×900 的日常办公视口。
- 完整三栏结构：左侧路径、中央工作区、右侧证据与审计。
- 支持鼠标悬停补充提示、键盘焦点、表格/列表更高密度阅读。
- 在 1280px 以下逐步收拢右侧栏，而不是压扁三栏。

### 未来电脑客户端

- 沿用电脑网页的信息架构和组件体系。
- 预留更稳定的窗口宽度、鼠标键盘操作和附件预览空间。
- 当前不指定桌面壳技术，不因未来客户端改造现有业务接口或页面状态机。

### iOS 手机端

本轮不出 iOS 专属界面、不做 SwiftUI 设计和实现。后续若启动，沿用移动端的信息结构，再做 iOS 原生规范适配。

## 6. 背景图片策略

用户后续会提供背景图片。本轮设计仅保留背景位与替换规则，不使用来历不明的安全生产场景图。

- 背景图片只作为环境层，不能承载文字、状态、按钮或证据内容。
- 所有正文、操作和状态必须放在独立的高对比内容层内。
- 图片须提供电脑横版和移动竖版的安全裁切区域；人物、设备或重点场景不得被标题与操作遮挡。
- 背景需加深色遮罩或实体阅读面板，保证弱光、复杂照片下仍可阅读。
- 如背景可能包含现场隐私、人员肖像、车牌或敏感设备信息，上传和发布前由业务方完成脱敏确认。

## 7. 非目标与边界

- 本轮不改变现有隐患状态机、角色定义、服务接口、数据结构。
- 本轮不将 M1 Mock 身份切换设计为生产功能；生产环境必须接入真实认证与权限体系。
- 本轮不承诺真实设备上的帧率数据；动效以可降级、低性能负担为原则。
- 本轮不增加“炫酷大屏”、粒子特效、连续发光和与业务无关的数据可视化。

## 8. 验收标准

- 任意角色打开任务详情后，能在 3 秒内识别任务标题、风险等级、当前节点、责任方、时限和下一步操作。
- 任务路径完整覆盖上报、交办、整改、复核、闭环；退回整改有明确回流表达。
- 同一任务在电脑端纵向路径和小程序横向路径中显示相同的业务状态。
- 375px 宽移动屏与 1280px、1440px 宽电脑屏均无关键内容被裁切、重叠或不可操作。
- 重大、临期、逾期、退回、闭环状态不只依赖单一颜色传达。
- 背景替换后，文字与关键操作仍保持清晰；背景不遮挡、不承载业务信息。
- 设计稿交付包含正常态、加载态、空态、异常态、无权限态和关键表单反馈态。

## 9. Open Design 交付清单

- 电脑网页：1440×900 完整三栏任务工作台、1280×800 收拢版。
- 微信小程序：375×812 和 430×932 两个移动基准尺寸。
- 首页、上报、详情、整改提交、复核/闭环、履职、我的的高保真页面。
- 节点路径、任务卡、风险标记、证据卡、审计时间线、主/次按钮、空/错/权限状态的组件与状态表。
- 色彩、字号、间距、圆角、阴影、边框、动效时长的设计 token。
- 使用真实背景图片后的裁切、遮罩、内容安全区示例。

## 10. 潜在风险分析

| 风险等级 | 风险 | 应对 |
| --- | --- | --- |
| 高 | 为了视觉效果改造既有状态机，导致设计与实际业务流转不一致 | 以第 3 节状态机为唯一依据；设计评审时逐节点对照 |
| 高 | 现场背景图影响文字可读性或泄露敏感现场信息 | 背景仅作环境层；增加遮罩和内容板；上线前人工脱敏审核 |
| 中 | 直接缩小桌面三栏造成小程序文字和点击区域不可用 | 使用重排规则，移动端改为单列与横向节点路径 |
| 中 | 高亮、模糊、粒子等效果在低端设备上影响操作 | 采用静态材质与轻量 transform/opacity 动效，并提供降级 |
| 低 | 后续桌面客户端框架变化影响视觉交付 | 保持组件、token 和信息架构平台无关，客户端沿用 H5 结构 |

本轮第一性原理：已启用。
